# Discovery Caveats

The graph was learned from shift-level summaries, so it cannot resolve within-shift ordering. Several operational variables are likely co-managed by supervisors. The output is useful for prioritizing bottleneck hypotheses, not for proving that one lever caused throughput changes.
